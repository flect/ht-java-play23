box: mihkels/java8-oracle@0.0.1
#services:
#    - mies/rethinkdb
build:
    steps:
        # Execute the bundle install step, a step provided by wercker
        - script:
            name: run test
            code: |-
                ./activator -Dsbt.ivy.home=$WERCKER_CACHE_DIR test
        - script:
            name: copy report
            code: |-
                cp -r ./target/test-reports $WERCKER_REPORT_ARTIFACTS_DIR/
